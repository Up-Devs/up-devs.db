const Error = require('../Error')

const EventEmitter = require('events');
const Events = require('../manager/Events');

/**
 * Stores values in a Redis database.
 */
class RedisDB extends EventEmitter {
  
  
  
  
  
}

module.exports = RedisDB;
