const BaseClient = require("./BaseClient");

/**
 * updev.db control client.
 * @extends {BaseClient}
 */
class Client extends BaseClient {

    /**
     * updev.db control client.
     * @param {ClientOptions} [options={}] - updev.db client options for this client.
     */
    constructor(options) {
    }
}

module.exports = Client