const EventEmitter = require('events');

/**
 * The Client class that used for event emitting.
 * @extends {EventEmitter}
 */
class BaseClient extends EventEmitter {

}

module.exports = BaseClient